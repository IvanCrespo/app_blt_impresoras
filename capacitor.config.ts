import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prueba_impresora',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
